'''
Write a Python program that:
Converts the given integer 97409 to its binary representation. Prints the binary representation.
'''

dec = 97409

# Initialize the binary string
binary = ""

# Calculate each binary digit manually
binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary
dec = dec // 2

binary = str(dec % 2) + binary

print(binary)